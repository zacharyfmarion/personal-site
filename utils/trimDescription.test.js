// import trimDescription from './trimDescription';

// describe('trimDescription', () => {
//   it('returns the string plus elipses if the length param is greater than the string length', () => {
//     expect(trimDescription('testing', 10)).toBe('testing...');
//   });
//   it('returns the shortened string plus elipses if the length param is less than the string length', () => {
//     expect(trimDescription('testing', 4)).toBe('test...');
//   });
// });
